{
  "name": "full-stack-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "frontend": "npm run dev --prefix frontend",
    "backend": "npm run dev --prefix backend/api-server-starter",
    "dev": "concurrently \"npm run frontend\" \"npm run backend\""
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  },
  "dependencies": {
    "caniuse-lite": "^1.0.30001663",
    "dotenv": "^16.4.5"
  }
}
